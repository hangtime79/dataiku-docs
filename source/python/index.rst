DSS and Python
###############

DSS includes deep integration with Python. In many parts of DSS, you can write Python code:

* In recipes
* In Jupyter notebooks
* In standard webapp backends
* In scenarios, metrics and checks
* In plugins
* For custom models in visual ML
* In API node, for :doc:`custom prediction models </apinode/endpoint-python-prediction>` or :doc:`custom functions </apinode/endpoint-python-function>` endpoints

Any Python package may be used in DSS.

In addition, DSS features a complete Python API, which has its own :doc:`complete documentation <devguide:api-reference/python/index>`.

The following highlights how a few specific Python packages can be used in DSS. DSS features advanced integration with most of the packages described below.

.. toctree::

	packages
	reusing-code
	matplotlib
	spacy
	bokeh
	plotly
	ggplot
	ipywidgets
