:orphan:

Specific data processing
########################

To avoid loosing or misinterpreting insights and meaning from the data, and negatively affecting the accuracy and performance of the models built on top of it, it is important to leverage the right techniques and tools when handling specific data types.

Dataiku provides tools to manipulate and process the following types of data:

.. toctree::
    :maxdepth: 1

    ../time-series/index
    ../geographic/index
    ../generative-ai/index
    ../nlp/index
    ../unstructured-data/image/index
    ../graph/index
    ../unstructured-data/audio/index
    ../unstructured-data/video/index
